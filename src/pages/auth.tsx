import { useState } from "react";
import { LoginForm, RegisterForm } from "@domain";

const AuthPage = () => {
    return (
        <main>
            <LoginForm />
        </main>
    );
};

export default AuthPage;
