import Head from "next/head";

const HomePage = () => {
    return (
        <main className="home-page">
            <Head>
                <title>Home | ConnectMe</title>
            </Head>
        </main>
    );
};

export default HomePage;
